# clean 合并边界

2026-09-22 将 `wess09/AzurPilot` 的 master 提交 `ae826868e`（Dev #1029）合入个人 deploy，前一版本为 `0be7e14e3`。PR #1000 已被上游合并。本部署以 master 为上游基线，不包含尚未进入 master 的后续 dev 提交。

沿用既有 clean 裁剪范围；本版本不承诺完全禁止公网通信。

## 已移除的链路

- 遥测 API 客户端、CL1 云端提交、错误日志上传、日报生成及发送。
- WebUI 在线更新器、Git over CDN 客户端及对应 API、导航、配置字段。
- WebUI 的 SSH 反向隧道/WebRTC 远程访问、远控状态 API 与页面；Discord 富状态集成。
- MCP SSE 服务、API 挂载、后台生命周期及此次上游新增的 `module/mcp/` 执行入口。
- 公网 NTP 校时；时间源只使用本机时钟。
- Microsoft Clarity 脚本与默认第三方壁纸请求；默认壁纸随程序分发为 `/wallpaper.jpg`。

保留上游的进程回收、配置事务、设备故障恢复、游戏任务修复和新界面功能。设备身份仍用于本地数据库加密与缓存，不上传；迁移时必须与数据库一并保留。

## 保留的通信及边界

| 功能 | 范围 |
| --- | --- |
| WebUI、模拟器、ADB、OCR RPC | 保留本地及用户配置的设备通信（包括用户指定的模拟器 SSH）；WebUI 保留原密码与监听设置 |
| LLM 分析、通知推送 | 仅按用户配置使用；可能将相应内容发送到用户配置的服务 |
| 游戏服务器状态 | 保留 `server-checker.nanoda.work` 查询及连接探测 |
| 作战委托维护信息 | 保留 `api-blhx-maintain.nanoda.work` 维护公告查询 |
| 活动资料 | 保留 Wiki 查询 |
| 自定义背景 | 默认本地；用户主动填写外部 URL 时仍可请求该地址 |
| 依赖安装、手动部署 | 保留 uv/npm 安装及显式 Git 部署工具；没有后台代码更新器 |

依赖清单仍可能包含供其他功能使用或上游共用的协议库；判断远控是否存在以服务入口、调用链和实际监听为准。未使用的 Discord `pypresence` 已从声明及锁文件移除。

## 验证

- Python 3.14.6 容器中禁网运行全量单元测试：977 项，成功（2 项平台检查跳过）。
- 前端 140 项单元测试、TypeScript 检查与生产构建通过。
- 真实 API 浏览器测试 16 项通过，覆盖配置并发保存、重连、表单和页面请求；clean 检查确认默认界面仅加载同源资源且没有更新与远控入口。
- 模拟浏览器测试 21 项通过；删除实例检查兼容上游的有效跳转落点，本地壁纸失败降级仍有覆盖。
- 配置与 API 契约已重新生成；Ruff 基础检查通过；运行时导入检查 509 个模块通过、3 个弃用生成器按既有白名单处理，意外失败为零。

这些自动化验证使用隔离夹具，未额外启动真实游戏任务。实际部署数据迁移、健康状态及回滚方法记录于生产目录的 README.md。
