# clean 合并边界

本次将 `wess09/AzurPilot` 的 dev 提交 `6e8b93c7b2b1c94107eb8f4e2de3ad1732e8ac2d`（2026-09-19）合入本地维护历史，基线为 deploy 的 `0379d948ab2873a1b26a2b19d136e4aae0da4bae`。

用户已明确选择“仅保持既有 clean 裁剪范围”。本版本因此不承诺完全禁止公网通信。

## 保留的裁剪

- 不恢复遥测、CL1 云端提交、错误报告上传、日报生成及发送链路。
- 不恢复在线更新、Git over CDN 客户端、远程访问、Discord 集成和 MCP 服务。
- NTP 保持本地时钟实现，不执行公网校时。
- React 的 API、后台生命周期、任务启动和导航均移除更新器依赖；已登录的旧客户端调用更新方法仍返回 `METHOD_NOT_FOUND`。
- 移除新首页的 Microsoft Clarity 脚本；公网壁纸改为旧版本随程序分发的本地图片。

## 允许的功能

保留模拟器、ADB、OCR RPC 和本地 WebUI 通信；按既有范围保留可选 LLM 分析、通知推送、服务器状态查询、活动信息查询以及安装依赖等功能。此次上游新增的作战委托维护查询属于游戏业务查询，仍保留。需要完全禁止公网通信时，必须另行收紧这些入口。

旧 WebUI 的纯本地统计迁移到新 React 界面。恢复委托收益区间汇总函数以支持新统计页，该函数仅读取本地数据库，不恢复日报服务。

## 验证

- Python 单元测试：521 项，成功；其中 2 项 Windows 专用检查在 Linux 跳过。
- 导入检查：490 个模块通过、3 个已明确弃用的生成器按既有白名单处理，意外失败为零。
- 前端构建、58 项前端单测及 14 项浏览器端到端测试通过。
- 浏览器检查实际页面请求，确认只加载同源资源且没有更新入口。
- 配置、翻译、按钮资源和 API 契约已重新生成；Ruff 检查通过。

上述本机 Python 验证使用 3.14.5；上游声明和锁文件仍要求 3.14.6。新增 Lua 解析器依赖及其配套 OmegaConf 使用上游锁文件版本。Docker 环境的实际版本与验证结果见部署记录。

## 代码审查

### 发现的问题

上游迁移目录后重新引入了已裁剪服务的调用，新 HTML 入口还包含 Clarity。此前裁剪的本地统计辅助函数已被新统计页使用。Windows 专用测试未区分运行平台。

### 建议修正

以上问题均已处理，并补充新路径、前端入口、API 拒绝更新请求及浏览器实际请求的回归检查。

### 是否需要继续修改

本次合并的 clean 边界检查已通过；未连接真实模拟器执行游戏回归。
